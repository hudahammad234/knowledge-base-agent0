# Core
numpy>=1.26

# Document processing
pypdf>=5.0
python-docx>=1.1

# Production LLM backend (optional - the assistant falls back to fully
# offline modes for embeddings/generation/validation if this is not
# installed or GEMINI_API_KEY is not set)
google-generativeai>=0.7

# Used only to generate the sample PDF in data/sample_docs (not required
# to run the assistant itself)
reportlab>=4.0
